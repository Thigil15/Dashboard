name: Test Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:  # Permite execução manual

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run simple test
        run: |
          echo "✅ Workflow está funcionando!"
          echo "Repositório: ${{ github.repository }}"
          echo "Branch: ${{ github.ref }}"
